# Build artifacts - generated during CI, never committed
# Backend serves manifests dynamically via API
macos/
windows/
linux/
version.txt

# Keep static GitHub Pages manifests for legacy client compatibility
!releases.json
!latest.json

# Keep only the static HTML index
!index.html
