name: Deploy Static Manifests to GitHub Pages

# Deploy static update manifests as compatibility mirrors for older clients.
# The future contract is reserved for https://updates.rostoc.co and is not served here yet.

on:
  push:
    branches:
      - main
    paths:
      - 'updates/*.json'
      - 'updates/index.html'
      - '.github/workflows/deploy-manifests.yml'
  workflow_dispatch:

# Sets permissions for GitHub Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write

# Single deployment at a time
concurrency:
  group: 'pages-manifests'
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
